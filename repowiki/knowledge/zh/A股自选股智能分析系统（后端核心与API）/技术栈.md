Python 3.10+/FastAPI + Uvicorn 作为API框架；Pydantic v2 用于请求/响应模型；SQLite（含WAL）作为轻量持久化；Electron + React + Vite + TypeScript 构建桌面/Web前端；LiteLLM 聚合多LLM提供商；dotenv 管理环境变量；pytest + playwright 进行单元测试与E2E测试；black/isort/bandit 代码规范。