主要运行方式：`python main.py` 执行单次分析，`python main.py --serve-only` 仅启动API，`python main.py --webui-only` 启动Web界面，`python server.py` 等价于 uvicorn 直接启动；前端需先在 `apps/dsa-web` 中执行 `npm install && npm run build`；桌面版通过 `scripts/build-all-macos.sh` 或 `build-all.ps1` 构建；Docker 部署使用 `docker/docker-compose.yml`。